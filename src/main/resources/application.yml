spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/DemoDB
    username: postgres
    password: shahirdb

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update